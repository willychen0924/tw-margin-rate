[project]
name = "tw-margin-rate"
version = "1.0.0"
description = "Portable stock-level Taiwan margin-maintenance history and website"
requires-python = ">=3.11"
dependencies = [
  "duckdb>=1.5,<2",
  "lxml>=5,<7",
  "numpy>=2,<3",
  "pandas>=2.2,<3",
  "pyarrow>=20,<26",
]

[tool.unittest]
start-directory = "tests"

