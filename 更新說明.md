# 更新說明：辦公室與家中兩台 Mac

這份說明給日常操作使用。兩台 Mac 使用同一份 iCloud `stock_data`，但同一時間只能由一台電腦更新與發布。

## 第一次在每台 Mac 準備

1. 登入同一個 iCloud 帳號，確認看得到 `stock_data`。
2. 安裝並測試「FinMind抓取器.app」，確認它產生 `raw/chips_margin`、`raw/prices`、`raw/stock_info` 的相同結構。
3. 取得本專案最新版，並用 Codex 開啟專案根目錄。
4. 在該台 Mac 個別設定 `FINMIND_TOKEN`。可以放在未納入 Git 的 `.env` 或系統環境變數；不要把實際 token 傳到聊天、文件或 GitHub。
5. 登入 GitHub，確認有權限更新 `willychen0924/tw-margin-rate`。在正式移轉完成前，本專案尚不能發布。
6. 確認 Python 與專案依賴已安裝；正式專案完成後應以專案提供的單一更新指令為準。

`FINMIND_TOKEN` 只有在共同原始資料缺最新日期時才需要。若抓取器已把資料完整寫入 iCloud，計算可直接使用 iCloud 資料。

## 每次更新前

1. 確認另一台 Mac 沒有正在執行抓取、計算、Git 提交或發布。
2. 等待 iCloud 同步結束。Finder 中若檔案仍有雲朵圖示，先選擇下載並等待完成。
3. 執行「FinMind抓取器.app」，等它正常結束。
4. 再等一次 iCloud 同步，避免 Codex 讀到只同步一半的 parquet。
5. 開啟 Codex 的「台股上市櫃融資維持率」專案。

## 可以直接對 Codex 說

```text
請依 AGENTS.md 更新融資維持率到最新完整交易日。先確認 iCloud 資料已下載並補齊缺漏，使用逐股平均成本法更新上市與櫃買，執行完整測試及本地瀏覽器檢查。先回報候選結果與差異；全部驗證通過後才發布，最後開公開網址核對。
```

若只想先檢查、不發布，請說：

```text
請更新並完整驗證，但不要提交、推送或發布。回報最新日期、四個數字與所有差異。
```

## Codex 應完成並回報的內容

更新完成前，Codex 應依序確認：

1. iCloud 三類必要資料的最新日期與實際下載狀態。
2. 是否使用 FinMind 補抓、補了哪些交易日及各 dataset 筆數。
3. 逐股完整重算或已驗證增量續算的方式。
4. 最新交易日的上市維持率、加權指數、櫃買維持率、櫃買指數。
5. 上一發布日以前的歷史是否完全一致；不一致時列出第一個差異日期並停止發布。
6. processed JSON 與網站嵌入資料是否一致。
7. 自動測試結果，以及本地桌面／窄螢幕、線性／對數、提示框與頁尾檢查結果。
8. 若發布：commit、公開網址，以及公開頁面再次核對結果。

## 人工核對網站

公開網址：`https://willychen0924.github.io/tw-margin-rate/`

請至少看以下項目：

- 標題是「台股上市櫃融資維持率」。
- 上圖為上市、下圖為櫃買。
- 摘要卡、圖例、資料日期與滑到最右端的提示數字一致。
- 130% 警戒線存在，但線旁沒有額外的 130% 小標。
- 線性／對數按鈕都能切換。
- 日期顯示中文；左右座標標題直排。
- 提示框貼近圖表下緣左右移動，不會把圖表撐高。
- 手機或窄視窗沒有漏白、圓角破洞或多餘小標籤。

可在網址後加查詢字串避免瀏覽器快取，例如 `?v=YYYYMMDD`。

## 常見狀況

### Finder 還有雲朵圖示

不要開始計算。先下載 `stock_data` 必要年份與最新月份；即使檔名看得到，內容仍可能尚未在本機。

### 三種資料的最新日期不同

不要用較新的價格配較舊的融資資料，也不要只更新其中一個市場。重新執行抓取器或讓 Codex 用 FinMind 補齊；仍不一致就停止。

### 顯示 `FINMIND_TOKEN` 未設定

若 iCloud 已完整到最新交易日，可要求使用現有資料、不連線補抓。若確實缺資料，在該台 Mac 設定 token；不要把 token 貼進文件或提交到 Git。

### 歷史值與前版不同

不要發布。請 Codex 保存候選輸出，列出第一個不同日期、兩市場最大差異與程式／資料變化。特別檢查暖機起點 2001 與 2015 的已知差異。

### GitHub 推送成功但網站沒變

等待 Pages 部署完成，再用帶日期查詢字串的公開網址重開。仍不一致時檢查 Pages 的來源分支／`docs` 設定與部署紀錄；不能只因 push 成功就宣告完成。

### iCloud 出現「衝突的拷貝」

立即停止兩台 Mac 的更新，不要任意刪除其中一份。保留所有衝突檔，先比較日期、大小與內容，再由 Codex 協助決定如何合併。

## 目前正式專案狀態

2026-08-03 已完成獨立移轉：

- 正式位置為 `Path.home() / "Library/Mobile Documents/com~apple~CloudDocs/台股上市櫃融資維持率"`。
- 必要程式、精簡 FinMind client、固定市場沿革 snapshot、官方更新器、processed JSON、網站與 16 項專用測試都在專案內。
- iCloud `stock_data` 保持同層獨立共用；沒有複製大型 raw archive。
- 正式暖機固定為 2001-01-05。2015 A/B 的早期歷史不完全一致，不能使用。
- iCloud 路徑已實際完整重算到 2026-07-30；2,207 個共同日期逐列與舊版一致。
- Git 已連接 `willychen0924/tw-margin-rate`，但移轉過程沒有推送或發布新版本。

完整分類、A/B 與刪除紀錄見 `移轉紀錄.md`。
