# 更新說明：辦公室與家中兩台 Mac

這份說明給日常操作使用。兩台 Mac 使用同一份 iCloud `stock_data`，但同一時間只能由一台電腦更新與發布。

本專案不需要安裝或執行「FinMind抓取器.app」。日常更新入口會直接讀取共同的 iCloud `stock_data`；若缺少最新交易日，才會使用該台 Mac 的 `FINMIND_TOKEN` 向 FinMind 逐日補抓逐股資料。網站新增的上市融資金額線固定由 FinMind 市場總額資料更新，櫃買融資金額線則由 TPEx 官方資料更新。

## 第一次在每台 Mac 準備

1. 登入同一個 iCloud 帳號，確認看得到 `stock_data`。
2. 取得本專案最新版，並用 Codex 開啟專案根目錄。
3. 在該台 Mac 個別設定 `FINMIND_TOKEN`。放在系統環境變數，或該台 Mac 的 `Path.home() / "Library/Application Support/tw-margin-rate/.env"`；不要放在 iCloud 專案，也不要把實際 token 傳到聊天、文件或 GitHub。
4. 每台 Mac 各自設定 GitHub SSH 金鑰，並確認 `origin` 為 `git@github.com:willychen0924/tw-margin-rate.git`。私鑰只放在當台 Mac 的 `~/.ssh`，不放進 iCloud 或 Git；設定完成後，日常 `git push` 不需再輸入 GitHub 電子郵件驗證碼。
5. 日常一律使用專案提供的 `更新融資維持率.command`；第一次執行時會準備必要的 Python 相依套件。

`FINMIND_TOKEN` 除了補抓共同原始資料，也用來更新上市融資金額總額；因此加入融資金額線後，日常更新仍會短暫連線 FinMind。這不代表 iCloud 逐股資料不完整，也不需要執行「FinMind抓取器.app」。

GitHub SSH 是各台 Mac 的一次性設定。公司與家中電腦應各自使用不同金鑰；不要透過 iCloud 複製或共用私鑰。若金鑰遺失或電腦更換，從 GitHub 帳號移除舊公鑰後，在新電腦重新建立即可。

## 每次更新前

1. 確認另一台 Mac 沒有正在執行抓取、計算、Git 提交或發布。
2. 等待 iCloud 同步結束。Finder 中若檔案仍有雲朵圖示，先選擇下載並等待完成。
3. 開啟 Codex 的「台股上市櫃融資維持率」專案。
4. 執行 `更新融資維持率.command`，或用下方文字請 Codex 更新；入口會先檢查 iCloud 資料，必要時才直接向 FinMind 補抓。

## 可以直接對 Codex 說

```text
請依 AGENTS.md 更新融資維持率到最新完整交易日。先確認 iCloud 資料已下載並補齊缺漏，使用逐股平均成本法更新上市與櫃買，執行完整測試及本地瀏覽器檢查。先回報候選結果與差異；全部驗證通過後才發布，最後開公開網址核對。
```

若只想先檢查、不發布，請說：

```text
請更新並完整驗證，但不要提交、推送或發布。回報最新日期、四個數字與所有差異。
```

## Codex 應完成並回報的內容

更新完成前，Codex 應依序確認：

1. iCloud 三類必要資料的最新日期與實際下載狀態。
2. 是否使用 FinMind 補抓、補了哪些交易日及各 dataset 筆數。
3. 逐股完整重算或已驗證增量續算的方式。
4. 最新交易日的上市維持率、加權指數、上市融資金額、櫃買維持率、櫃買指數、櫃買融資金額。
5. 上一發布日以前的歷史是否完全一致；不一致時列出第一個差異日期並停止發布。
6. processed JSON 與 `docs/index.html`、根目錄 `index.html` 的嵌入資料是否一致，且兩份 HTML 是否完全相同。
7. 自動測試結果，以及本地桌面／窄螢幕、四條線圖例開關、提示框與頁尾檢查結果。
8. 若發布：commit、公開網址，以及公開頁面再次核對結果。

## 人工核對網站

公開網址：`https://willychen0924.github.io/tw-margin-rate/`

請至少看以下項目：

- 瀏覽器分頁標題是「台股上市櫃融資維持率」；頁面不顯示獨立大標題，頂端為精簡 kicker、資料日期與兩張摘要卡。
- 上圖為上市、下圖為櫃買。
- 摘要卡、圖例、資料日期與滑到最右端的提示數字一致。
- 櫃買指數統一顯示一位小數（例如 `390.8`）；上市加權指數維持整數顯示。
- 130% 警戒線存在，但線旁與圖例都沒有「130% 警戒線」文字。
- 圖表固定使用線性刻度，不顯示線性／對數按鈕。
- 預設期間為 `1y`。維持率、指數、融資餘額與融資市值比四個圖例可直接點擊開關；上下兩圖各自的開關狀態保存在該瀏覽器。手機圖例名稱精簡為「維持率／指數／餘額／市值比」，桌機保留完整名稱。融資餘額為綠色實線並以如 `5470.1億` 顯示，融資市值比保留兩位小數。
- 上市市值自 2017-07-03 起只加總四位數普通股，排除 ETF、TDR 與特別股，依官方上市／創新板／終止上市日期重建每日市場歸屬；一般上市日期以公司基本資料為準，創新板起點及已下市公司缺漏再由申請上市歷史補足。完整 TWSE 週報與最近總市值保留作口徑差檢查，另以每月最後週報日及最近交易日的官方 `MI_INDEX` 代號清單做普通股等口徑驗證。櫃買市值自同日起使用 TPEx 官方每日個股市值完整回填。
- 手機版兩張最新數值卡上下排列，左側分別顯示上市藍、櫃買金細色條，且沒有裝飾藍點。
- 四條資料線採參考版藍、橘、綠、紫配色；期間選擇列維持精簡高度。
- 融資維持率主線較粗，其他三線使用 70% 透明度，維持主次層級並避免過淡；桌機線寬依 B 版實際畫面調整為主線 `2.5`、其他線 `1.4`，四個末端圓點縮為 `3.2px`。
- 手機版不顯示「維持率／指數」軸標題，左右及下軸刻度加大至 `18px` SVG 字級，右軸千位數以 `k` 縮寫；桌機三側刻度與底部軸標題均使用 `9px` SVG 字級、相同淡文字顏色及一般字重，底部「維持率／指數」與日期刻度共用同一基線。長按圖表約 0.32 秒後可拖曳十字準線與提示框，桌機仍以滑鼠 hover 操作。
- 桌機圖表縮減左右側繪圖留白，將資料繪圖區從 SVG `x=64..672` 擴大為 `x=39..712`，橫向寬度增加約 11%；手機版邊界不變。
- 桌機圖表卡片縮短日期軸下方留白；手機版卡片高度與下方留白維持不變。
- 頁面底色、白色卡片、主文字、次文字與淡文字使用 B 版暖米色系。
- 圖表標頭使用參考版的淺色 header band 與底部分隔線，「上市／櫃買」及四項圖例整體靠左；字級、字重、chip／圖例內距與項目間距對照參考 HTML，上市 chip 為淡藍底／深藍字，櫃買 chip 為淡金底／深金字；四色方塊與圖線開關保留，不顯示額外的操作提示文字。
- 上方摘要卡的漲跌膠囊改回參考 HTML 配色：上漲紅字／淡紅底，下跌綠字／淡綠底；背景透明度由 `10%` 略提高為 `14%`，讓底色稍微明顯。
- 維持率漲跌膠囊只顯示箭頭與一位小數（例如 `▼ 4.0`），不顯示 `pt`；融資餘額漲跌仍顯示 `億`。
- 手機版 header band 左側固定市場 chip，右側以兩欄兩列顯示：維持率／指數在第一列，餘額／市值比在第二列；各組名稱與數值保持同行，兩列間距縮為 `0`、圖例上下內距縮為 `1px`，整體維持靠左排列。
- `360px` 以下窄螢幕會小幅縮小 chip、色塊、圖例字級與欄距，保留相同排列並避免水平溢出。
- 日期顯示中文；桌機底部依序安排「維持率」、日期刻度與「指數」，其中「維持率」與左軸數字欄齊左，「指數」與右軸數字欄齊左。
- 桌機滑鼠 hover 與手機長按提示框的上緣略高於圖表最下方的維持率刻度數字，可覆蓋該刻度、部分圖表下緣與日期並跨出卡片，且不會把圖表或卡片撐高。
- 手機或窄視窗沒有漏白、圓角破洞或多餘小標籤。

可在網址後加查詢字串避免瀏覽器快取，例如 `?v=YYYYMMDD`。

## 常見狀況

### Finder 還有雲朵圖示

不要開始計算。先下載 `stock_data` 必要年份與最新月份；即使檔名看得到，內容仍可能尚未在本機。

### 三種資料的最新日期不同

不要用較新的價格配較舊的融資資料，也不要只更新其中一個市場。讓單一更新入口或 Codex 用 FinMind 補齊；仍不一致就停止。

### 顯示 `FINMIND_TOKEN` 未設定

若 iCloud 已完整到最新交易日，可要求使用現有資料、不連線補抓。若確實缺資料，在該台 Mac 設定 token；不要把 token 貼進文件或提交到 Git。

### 歷史值與前版不同

不要發布。請 Codex 保存候選輸出，列出第一個不同日期、兩市場最大差異與程式／資料變化。特別檢查暖機起點 2001 與 2015 的已知差異。

### GitHub 推送成功但網站沒變

等待 Pages 部署完成，再用帶日期查詢字串的公開網址重開。專案同時維護 `docs/index.html` 與完全相同的根目錄 `index.html`，以相容 Actions 與 `main` 根目錄兩種 Pages 來源；若兩者不同就停止發布。不能只因 push 成功就宣告完成。

### iCloud 出現「衝突的拷貝」

立即停止兩台 Mac 的更新，不要任意刪除其中一份。保留所有衝突檔，先比較日期、大小與內容，再由 Codex 協助決定如何合併。

## 目前正式專案狀態

2026-08-03 已完成獨立移轉；2026-08-04 已驗證更新至 2026-08-03 交易日：

- 正式位置為 `Path.home() / "Library/Mobile Documents/com~apple~CloudDocs/台股上市櫃融資維持率"`。
- 必要程式、精簡 FinMind client、固定市場沿革 snapshot、官方更新器、processed JSON、網站與完整專用測試都在專案內。
- iCloud `stock_data` 保持同層獨立共用；沒有複製大型 raw archive。
- 正式暖機固定為 2001-01-05。2015 A/B 的早期歷史不完全一致，不能使用。
- iCloud 路徑已從 2001-01-05 實際完整重算到 2026-08-03；2,209 個顯示日期，結果與目前正式 JSON 逐列一致。
- 1459 聯發與 1589 永冠-KY 的 TWSE 市場沿革已依官方上市日期修正；目前 2026-07-30 回歸基準為上市 `132.50`、櫃買 `118.32`。
- Git 已連接 `willychen0924/tw-margin-rate`，並由 `main` 發布到既有 GitHub Pages。

完整分類、A/B 與刪除紀錄見 `移轉紀錄.md`。
