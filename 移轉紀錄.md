# 2026-08-03 安全移轉紀錄

## 目標與邊界

- 正式專案：`Path.home() / "Library/Mobile Documents/com~apple~CloudDocs/台股上市櫃融資維持率"`。
- 共用原始資料：正式專案同層的 `stock_data`，保持獨立，不複製 raw archive。
- 舊專案：`Path.home() / "Documents" / "價值股追蹤"`。
- 未複製 `.env`、token、Codex auth、虛擬環境、完整共用 cache 或價值篩選報表。

## 來源分類

### 融資維持率專用；驗證通過後精確刪除

```text
.github/workflows/pages.yml
scripts/build_margin_maintenance_history.py
scripts/build_margin_maintenance_proxy_history.py
scripts/update_margin_maintenance_chart_data.py
data/processed/margin-maintenance-history.json
data/processed/recent-stock-level-margin-estimates.json
docs/.nojekyll
docs/index.html
```

`build_margin_maintenance_proxy_history.py` 與 `recent-stock-level-margin-estimates.json` 是已停用 proxy 流程，只保留在本紀錄的刪除清單，不移入正式專案。

### 必須複製成專案內版本；來源仍須保留

- `src/value_screener/finmind.py`：價值篩選流程共用；新專案複製為 `src/tw_margin_rate/finmind.py` 並更換專案識別。
- 舊專案 `data/cache` 中 2026-07-27 至 2026-07-30 的 4 個融資與 4 個價格快取：只複製約 2.2 MB 的基準必要補充檔；來源完整共用 cache 保留。
- 舊 `pyproject.toml` 的資料處理依賴：新專案重建獨立 pyproject；來源檔保留。

### 價值篩選／動能共用或無關；不可刪除

- `.env`、`.gitignore`、README、原專案 `.git`。
- `src/value_screener/**` 全部模組。
- `data/cache/**` 全部快取。
- `config/**`、`reports/**`、`outputs/**`、`qa/**`。
- `scripts/run_screen.py`、`run_all.sh`、報表產生器、排名及清理腳本。
- `tests/**` 既有 16 項價值篩選／動能測試。

## 市場沿革可重現輸入

7/30 基準所用兩份檔案已固定保存並記錄 SHA-256：

- TWSE 上市公司基本資料：`data/reference/twse-company-info.2026-07-30.json`。
- TWSE 終止上市公司：`data/reference/twse-delisted.2026-07-30.html`。

來源 URL 與雜湊在 `data/reference/manifest.json`；`scripts/fetch_market_reference.py` 可由 TWSE 官方端點原子更新 `.latest` 版本。更新後仍須先通過舊日期逐列不變性檢查。

## 暖機 A/B

固定相同 iCloud archive、相同 7/30 市場沿革 snapshot、相同逐股算法與 2026-07-30 終點：

- 2001-01-05：TWSE／TPEx 各 2,207 列，所有列與舊 processed JSON 完全一致。
- 2015-01-01：TWSE 44 列、TPEx 30 列相差 0.01pp；第一筆分別為 2017-07-03、2017-07-04。
- 兩種暖機的 7/30 四個最新值相同，但 2015 不符合完整歷史不變條件。

結論：正式暖機固定為 `2001-01-05`。

## 移轉驗證

- 從 iCloud 正式路徑執行 `更新融資維持率.command --end 2026-07-30 --no-finmind-fetch` 成功。
- 完整處理 2001-01-05 至 2026-07-30 共 6,275 個資料交易日。
- 輸出 2,207 個上市／櫃買共同顯示日期。
- 7/30：上市 132.49、加權 39,933.30；櫃買 118.34、櫃買 326.23。
- 新舊所有市場歷史列完全一致；metadata 僅新增算法版本與參考檔名稱／雜湊，並更新產生時間。
- 16 項新專案專用測試通過；JSON 與 HTML 嵌入歷史、卡片、圖例、日期一致。
- 本機瀏覽器已檢查桌面與 390px 窄螢幕；線性／對數切換正常、無水平溢出或 console error、雙圖與頁尾正常。

## 刪除狀態

已完成。刪除前再次排除上述八個專用檔後搜尋整個來源專案，沒有共用程式引用它們；新舊歷史、HTML、更新腳本與 workflow 也已完成比對。

八個檔案已從舊專案移到可恢復的垃圾桶資料夾：

```text
Path.home() / ".Trash" / "tw-margin-rate-source-cleanup-20260803"
```

沒有刪除來源目錄，也沒有刪除共用 `src/value_screener/finmind.py`、`data/cache`、設定、報表或價值／動能腳本。清理後：

- 舊價值篩選專案原有 16 項測試全部通過。
- iCloud 融資維持率專案 16 項專用測試全部通過。
- iCloud processed JSON／HTML 基準驗證再次通過。
