# 融資維持率計算方式

## 目的與定義

本專案估算的是「市場整體融資部位，以逐股移動平均成本計算的維持率」。它不是券商對單一客戶帳戶計算的追繳維持率，也不是 FinMind 市場總維持率的 proxy。

計算分成兩層：先逐日維護每檔股票的融資平均成本，再把合格普通股按上市與櫃買分開加總。

## 使用資料

| 用途 | iCloud 位置 | FinMind dataset | 必要欄位 |
|---|---|---|---|
| 融資買賣與餘額 | `raw/chips_margin` | `TaiwanStockMarginPurchaseShortSale` | `date`, `stock_id`, `MarginPurchaseBuy`, `MarginPurchaseSell`, `MarginPurchaseCashRepayment`, `MarginPurchaseTodayBalance`, `MarginPurchaseYesterdayBalance` |
| 收盤價與指數 | `raw/prices` | `TaiwanStockPrice` | `date`, `stock_id`, `close` |
| 當前市場別 | `raw/stock_info` | `TaiwanStockInfo` | `stock_id`, `type`, `date` |

`TaiwanStockPriceAdj` 是還原價格資料，可用於除權息研究及額外驗證；現行融資平均成本公式使用實際交易日的未還原收盤價。改用還原價會改變成本口徑，必須另行驗證與核准。

轉上市、已下市股票不能只靠最新一份 `TaiwanStockInfo` 正確回推全部歷史市場別。現行來源程式還讀取 TWSE 上市日期與下市公司代號；正式移轉必須把這兩份參考資料的取得、快取日期與測試一併納管。

## 股票篩選

每個交易日只納入：

- `stock_id` 恰為四個阿拉伯數字；
- 非 `0` 開頭（排除 ETF 等商品）；
- 非 `91` 開頭（排除 TDR）；
- 當日融資餘額大於 0；
- 可判定為 `twse` 或 `tpex`。

上市與櫃買分開彙總。同一股票同一天只能屬於一個市場。

## 逐股平均成本

對股票 `i`、交易日 `t` 定義：

- `B(t-1)`：昨日融資餘額；
- `B(t)`：今日融資餘額；
- `Buy(t)`：今日融資買進；
- `Sell(t)`：今日融資賣出；
- `Repay(t)`：今日現金償還；
- `P(t)`：今日收盤價；
- `C(t-1)`：昨日仍在庫融資部位的平均成本；
- `C(t)`：今日更新後的平均成本。

先算賣出與現償後的舊部位：

```text
Remaining(t) = max(B(t-1) - Sell(t) - Repay(t), 0)
```

若 `B(t) > 0`：

```text
C(t) = [Remaining(t) × C(t-1) + Buy(t) × P(t)] ÷ B(t)
```

這代表融資賣出與現償先按原平均成本減少部位，當日融資買進再以當日收盤價加入成本庫存。

### 初始、缺價與清倉規則

- 暖機第一天或某股票首次出現且沒有舊成本時，以當日收盤價作 `C(t-1)` 的初始化替代值。
- 若當日個股缺收盤價，現行來源程式沿用該股票最近一個可用收盤價；若連歷史收盤價也沒有，當日排除該股票。正式測試必須鎖定這項行為。
- 若 `B(t) = 0`，刪除該股票的平均成本狀態。日後重新出現時重新初始化，不能沿用清倉前成本。
- `Remaining(t)` 不得為負值。
- 融資張數只要分子與分母使用相同單位，維持率中的單位會相消；不可在部分欄位乘 1,000、部分欄位不乘。

## 市場維持率

對市場 `m` 當日所有合格股票集合 `S(m,t)`：

```text
市場價值(t) = Σ [P(i,t) × B(i,t)]
估計融資債務(t) = Σ [C(i,t) × B(i,t) × 0.60]

市場融資維持率(t)
  = 市場價值(t) ÷ 估計融資債務(t) × 100
```

亦即：

```text
Σ（收盤價 × 融資餘額）
──────────────────────────── × 100
Σ（平均成本 × 融資餘額 × 0.60）
```

`0.60` 是融資成數假設。分母為零或任一結果不是有限數時，該日不得發布。

## 市場別判定

現行來源程式的邏輯為：

1. 最新 `TaiwanStockInfo` 標為上市者，以 TWSE 上市日期判斷；上市日前的歷史資料歸櫃買，上市日後歸上市。
2. 最新資料標為櫃買者歸櫃買。
3. 最新資料已找不到的舊股票，若在 TWSE 下市清單則歸上市，否則暫歸櫃買。

第 3 點是 fallback，不應被視為完美的歷史主檔。移轉時需用已知轉板／下市個案建立測試，並評估改成可追溯日期的市場沿革表。

## 暖機、顯示與續算

- 正式暖機：`2001-01-05`。
- 網站第一個顯示日：`2017-07-03`。
- 暖機期間只建立逐股成本狀態，不輸出網站資料。
- 在沒有可靠 state 檔前，應由暖機日起完整重算。
- 若未來新增增量續算，state 至少要保存每檔平均成本、最近收盤價、最後交易日、資料版本與算法版本，而且必須證明增量結果與完整重算逐日完全一致。

### 2001／2015 暖機 A/B 結果

移轉時固定相同原始資料、程式與 2026-07-30 市場沿革 snapshot，分別完整重算：

| 暖機起點 | TWSE 對舊版 | TPEx 對舊版 | 7/30 最新值 |
|---|---:|---:|---|
| 2001-01-05 | 2,207 列完全一致 | 2,207 列完全一致 | 四個基準值完全一致 |
| 2015-01-01 | 44 列相差 0.01pp | 30 列相差 0.01pp | 四個基準值相同 |

2015 的第一筆差異在 TWSE 2017-07-03、TPEx 2017-07-04。因專案要求保留全部已發布歷史，正式暖機固定為 `2001-01-05`。

## 正式輸出

`data/processed/margin-maintenance-history.json` 應包含：

- `metadata.generated_at`、`source`、`warmup_start`、`display_start`、`end`、`formula`、`universe`、市場主檔版本；
- `markets.twse[]` 與 `markets.tpex[]`；
- 每列至少有 `date`、`maintenance`、`index`、`financed_balance`、`stock_count`。

兩市場日期應嚴格遞增、無重複且共同日期一致。`docs/index.html` 的圖表資料、摘要卡、圖例與資料日期必須全部由這一份 JSON 的最新共同日期產生。

## 已驗證回歸基準

2026-07-30：

| 市場 | 原始維持率 | 網站顯示 | 指數 |
|---|---:|---:|---:|
| 上市 | 132.49% | 132.5% | 加權指數 39,933.30 |
| 櫃買 | 118.34% | 118.3% | 櫃買指數 326.23 |

來源任務記錄當次 16 項既有價值篩選測試通過，且 2026-07-29 以前的歷史值與前版完全一致。新專案另有 16 項融資算法、路徑、基準與網站同步專用測試。
